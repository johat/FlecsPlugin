# General config

# By default let git handle line endings however it sees best for the OS
* text=auto

# Use windows native line endings for *.bat files
*.bat -crlf

# Use linux native line endings for *.sh files
*.sh -lf

# Use mac native line endings for *.command files
*.command -cr

# Git LFS config

# Unreal assets
*.umap filter=lfs diff=lfs merge=lfs -text
*.uasset filter=lfs diff=lfs merge=lfs -text

# Compiled libraries (where needed)
*.dll filter=lfs diff=lfs merge=lfs -text

# Image files (where needed)
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text

# Model files (where needed)
*.fbx filter=lfs diff=lfs merge=lfs -text
